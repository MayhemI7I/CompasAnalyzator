# 🧭 Compass Analyzer v2.0

**Система анализа калибровки компасов МТЦ**  
Desktop-приложение для анализа данных калибровки компасов с поддержкой пакетной обработки, истории и экспорта результатов.

---

## 📋 Содержание

1. [Возможности](#-возможности)
2. [Требования](#-требования)
3. [Быстрый старт](#-быстрый-старт)
4. [Где хранится история](#-где-хранится-история)
5. [Перенос на другой ПК](#-перенос-на-другой-пк)
6. [Использование](#-использование)
7. [Структура проекта](#-структура-проекта)
8. [Разработка](#-разработка)

---

## 🚀 Возможности

### ✅ Анализ калибровки
- **Единичный анализ** - анализ одной папки с данными компаса
- **Пакетный анализ** - автоматическая обработка множества папок (параллельная обработка)
- **Тип устройства** - указание типа ДСС (Коралл, Катран и т.д.) перед анализом
- **Запоминание типа** - последний введенный тип автоматически подставляется

### 📊 Визуализация
- **График углов** - scatter-диаграмма с подсветкой поворотов
- **Выбор диапазона** - просмотр данных по индексам
- **Подсветка поворотов** - разные цвета для каждого поворота
- **Таблица поворотов** - детальная информация о каждом повороте

### 📜 История анализов
- **Автоматическое сохранение** - все анализы сохраняются в историю
- **Фильтрация** - по статусу, дате, типу устройства
- **Поиск** - по номеру компаса с умной сортировкой
- **Сортировка** - по дате, номеру ДСС, типу устройства
- **Оптимизация памяти** - ленивая загрузка больших данных

### 📤 Экспорт результатов
- **CSV экспорт** - результаты калибровки в формате CSV
- **JSON экспорт** - полные данные в JSON
- **Выбор директории** - сохранение в любую папку
- **Запоминание пути** - последняя папка экспорта запоминается
- **Название файла** - включает тип устройства и дату

### 🛠️ Редактор файлов
- **Массовое переименование** - удаление текста из имен файлов
- **Рекурсивный поиск** - обработка вложенных папок
- **Предпросмотр** - показывает изменения до применения
- **Статистика** - количество успешных/неуспешных операций

### ⚙️ Настройки
- **Пороги стабильности** - настройка чувствительности анализа
- **Допуски поворотов** - допустимое отклонение угла поворота
- **Минимальная длина сегмента** - фильтрация коротких сегментов
- **Максимум выбросов** - количество допустимых аномалий

---

## 💻 Требования

### Для использования:
- **Windows** 10/11 (64-bit)
- **WebView2 Runtime** (обычно уже установлен в Windows 11)

### Для разработки:
- **Go** 1.21+
- **Node.js** 16+ (опционально, только для разработки frontend)
- **Wails CLI** v2.10.2+
- **WebView2 Runtime**

---

## 🎯 Быстрый старт

### 1️⃣ Запуск приложения

#### Вариант A: Готовый EXE-файл
```
1. Перейдите в папку: build\bin\
2. Запустите: CompassAnalyzer.exe
3. Готово! ✅
```

#### Вариант B: Сборка из исходников
```bash
# Клонируйте репозиторий
git clone <repository-url>
cd CompasAnalyzer

# Установите зависимости Go
go mod download

# Соберите приложение
wails build -f main_desktop.go

# Запустите
build\bin\CompassAnalyzer.exe
```

### 2️⃣ Первый анализ

1. **Выберите папку** с данными компаса (должна содержать `SB_CMPS.csv`)
2. **Введите тип устройства** (например: "Коралл")
3. **Нажмите "Начать анализ"**
4. Результаты отобразятся автоматически

---

## 📁 Где хранится история

### Windows:
```
C:\Users\<Имя_пользователя>\CompasAnalyzer\history\analysis_history.json
```

**Пример:**
```
C:\Users\Operator\CompasAnalyzer\history\analysis_history.json
```

### Структура папки:
```
C:\Users\<Username>\CompasAnalyzer\
├── history\
│   └── analysis_history.json    ← ВСЯ ИСТОРИЯ ЗДЕСЬ
```

### Размер файла истории:
- **100 анализов** ≈ 2-5 МБ
- **1000 анализов** ≈ 20-50 МБ
- **10000 анализов** ≈ 200-500 МБ

> 💡 **Совет**: Периодически делайте резервную копию файла `analysis_history.json`

---

## 💾 Перенос на другой ПК

### 📦 Что нужно скопировать на флешку:

#### Минимальный набор (только программа):
```
📁 CompasAnalyzer\
├── 📄 CompassAnalyzer.exe    ← Исполняемый файл
└── 📄 README.md              ← Документация (опционально)
```

#### Полный набор (программа + история):
```
📁 CompasAnalyzer\
├── 📄 CompassAnalyzer.exe           ← Исполняемый файл
├── 📄 README.md                     ← Документация
└── 📁 history\                      ← ПАПКА С ИСТОРИЕЙ
    └── 📄 analysis_history.json     ← Файл истории
```

---

## 🔄 Инструкция по переносу

### Метод 1: ПЕРЕНОС С ИСТОРИЕЙ (рекомендуется)

#### На исходном ПК:

1. **Скопируйте программу**:
   ```
   Папка проекта: C:\Projects\CompasAnalyzer\build\bin\
   Скопируйте: CompassAnalyzer.exe → на флешку
   ```

2. **Скопируйте историю**:
   ```
   Папка истории: C:\Users\<Username>\CompasAnalyzer\history\
   Скопируйте: analysis_history.json → на флешку в папку history\
   ```

3. **Структура на флешке**:
   ```
   E:\CompasAnalyzer\
   ├── CompassAnalyzer.exe
   └── history\
       └── analysis_history.json
   ```

#### На новом ПК:

1. **Вставьте флешку** → Диск E:\ (например)

2. **Скопируйте программу**:
   ```
   E:\CompasAnalyzer\CompassAnalyzer.exe
   ↓ Копировать в
   C:\Projects\CompasAnalyzer\CompassAnalyzer.exe
   ```

3. **Скопируйте историю**:
   ```
   E:\CompasAnalyzer\history\analysis_history.json
   ↓ Копировать в
   C:\Users\<НовыйПользователь>\CompasAnalyzer\history\analysis_history.json
   ```

   > ⚠️ **Важно**: Если папка `CompasAnalyzer\history\` не существует - создайте её!

4. **Запустите программу**:
   ```
   C:\Projects\CompasAnalyzer\CompassAnalyzer.exe
   ```

5. **Проверьте историю**:
   - Перейдите во вкладку "История"
   - Вся история должна отобразиться ✅

---

### Метод 2: ПЕРЕНОС БЕЗ ИСТОРИИ (быстрый)

1. **Скопируйте только EXE**:
   ```
   build\bin\CompassAnalyzer.exe → на флешку
   ```

2. **На новом ПК**:
   ```
   Скопируйте CompassAnalyzer.exe в любую папку
   Запустите
   ```

3. **Результат**:
   - Программа запустится с пустой историей
   - Новые анализы будут сохраняться

---

### Метод 3: ПОРТАТИВНЫЙ РЕЖИМ (запуск с флешки)

1. **Структура на флешке**:
   ```
   E:\CompasAnalyzer\
   ├── CompassAnalyzer.exe
   └── history\
       └── analysis_history.json
   ```

2. **Запуск**:
   ```
   Запустите: E:\CompasAnalyzer\CompassAnalyzer.exe
   ```

3. **⚠️ Ограничение**:
   - История всё равно будет сохраняться в:
     ```
     C:\Users\<Username>\CompasAnalyzer\history\
     ```
   - Для переноса истории используйте **Метод 1**

---

## 📚 Использование

### 1. Единичный анализ

```
1. Вкладка "Анализ"
2. Выберите папку с данными (кнопка 📁)
3. Введите тип устройства (например: "Коралл")
4. Нажмите "Начать анализ"
5. Результаты:
   - График углов
   - Таблица поворотов
   - Статистика
```

### 2. Пакетный анализ

```
1. Вкладка "Пакетный"
2. Укажите директорию с папками компасов
3. Введите тип устройства (общий для всех)
4. Нажмите "Пакетный анализ"
5. Результаты:
   - Таблица всех результатов
   - Фильтр успешных/неуспешных
   - Кнопка детального просмотра для каждого
```

### 3. История

```
1. Вкладка "История"
2. Фильтры:
   - Поиск по номеру компаса
   - Фильтр по статусу (успешно/не прошло)
   - Фильтр по типу устройства
   - Фильтр по датам
   - Сортировка (по дате/номеру/типу)
3. Кнопка "Просмотр" → детальный просмотр
4. Кнопка "Экспорт CSV" → выгрузка выбранных
5. Кнопка "Очистить историю" → удаление всех записей
```

### 4. Экспорт

#### CSV экспорт:
```csv
№ ДСС;Результат калибровки;Количество поворотов;Поворот 1;Поворот 2;...
1234;Успешно;4;90.12°;180.34°;270.45°;359.89°
```

**Название файла**:
```
Результаты калибровки "Коралл"_20251031_1234.csv
```

#### JSON экспорт:
```json
{
  "compass": "1234",
  "deviceType": "Коралл",
  "isValid": true,
  "turns": [...],
  "allAngles": [...]
}
```

### 5. Редактор файлов

```
1. Вкладка "Редактор файлов"
2. Выберите директорию
3. Укажите текст для удаления (например: "tim")
4. Выберите режим:
   - □ Рекурсивно (вложенные папки)
5. Кнопка "Предпросмотр" → показать изменения
6. Кнопка "Выполнить" → применить изменения
```

---

## 📂 Структура проекта

```
CompasAnalyzer/
│
├── 📁 build/
│   └── 📁 bin/
│       └── 📄 CompassAnalyzer.exe        ← Готовый исполняемый файл
│
├── 📁 desktop/
│   ├── 📄 app.go                         ← Основная логика (анализ, пакетная обработка)
│   ├── 📄 storage.go                     ← Работа с историей
│   └── 📄 file_editor.go                 ← Редактор файлов
│
├── 📁 analyzer/
│   └── 📄 analyzer.go                    ← Алгоритмы анализа калибровки
│
├── 📁 parser/
│   └── 📄 csv_parser.go                  ← Парсинг CSV файлов
│
├── 📁 webui/
│   └── 📁 static/
│       ├── 📄 index.html                 ← UI интерфейс
│       ├── 📄 app.js                     ← Логика frontend
│       ├── 📄 app_wails.js               ← Синхронизированная версия
│       └── 📄 styles.css                 ← Стили
│
├── 📄 main_desktop.go                    ← Точка входа приложения
├── 📄 wails.json                         ← Конфигурация Wails
├── 📄 go.mod                             ← Зависимости Go
└── 📄 README.md                          ← Документация
```

---

## 🛠️ Разработка

### Установка окружения:

```bash
# 1. Установите Go
https://go.dev/dl/

# 2. Установите Wails CLI
go install github.com/wailsapp/wails/v2/cmd/wails@latest

# 3. Установите зависимости проекта
go mod download
```

### Команды сборки:

```bash
# Режим разработки (с hot-reload)
wails dev

# Production сборка
wails build -f main_desktop.go

# Чистая пересборка
wails build -f main_desktop.go -clean
```

### Синхронизация файлов:

```bash
# После изменений в app.js - ОБЯЗАТЕЛЬНО:
Copy-Item "webui\static\app.js" -Destination "webui\static\app_wails.js" -Force
```

### Структура кода:

#### Backend (Go):
- `desktop/app.go` - API методы для frontend
- `desktop/storage.go` - Сохранение/загрузка истории
- `analyzer/analyzer.go` - Алгоритмы анализа
- `parser/csv_parser.go` - Парсинг CSV

#### Frontend (JavaScript):
- `app.js` - Основная логика UI
- `index.html` - Разметка интерфейса
- `styles.css` - Стили Material Design

---

## 🔧 Настройки по умолчанию

```javascript
{
  stabilityThreshold: 0.5,      // Порог стабильности угла
  turnTolerance: 5.0,           // Допуск поворота (±5°)
  minSegmentLength: 5,          // Минимальная длина сегмента
  maxOutliers: 2,               // Максимум выбросов
  sumTolerance: 10.0            // Допуск суммы углов
}
```

Настройки сохраняются в `localStorage` браузера.

---

## 📊 Производительность

### Оптимизации:

1. **Параллельная обработка** - использует все ядра процессора
2. **Ленивая загрузка** - история загружается по требованию
3. **Пакетное сохранение** - одна операция вместо тысяч
4. **Сжатие данных** - оптимизированный JSON

### Бенчмарки:

| Операция | Время | Память |
|----------|-------|--------|
| Анализ 1 компаса | 0.1-0.5 сек | 50 МБ |
| Пакетный анализ (100 папок) | 10-30 сек | 200 МБ |
| Загрузка истории (1000 записей) | 0.5-1 сек | 100 МБ |
| Экспорт CSV (1000 записей) | 1-2 сек | 150 МБ |

---

## ❓ FAQ

### Q: История не отображается после переноса?
**A:** Скопируйте файл `analysis_history.json` в правильную папку:
```
C:\Users\<НовыйПользователь>\CompasAnalyzer\history\
```

### Q: Программа не запускается?
**A:** Убедитесь, что установлен **WebView2 Runtime**:
```
https://developer.microsoft.com/en-us/microsoft-edge/webview2/
```

### Q: Как удалить всю историю?
**A:** 
1. Вкладка "История" → Кнопка "Очистить историю"  
   ИЛИ
2. Удалить файл вручную:
   ```
   C:\Users\<Username>\CompasAnalyzer\history\analysis_history.json
   ```

### Q: Можно ли запускать с флешки?
**A:** Да, но история будет сохраняться на локальном ПК в папке пользователя.

### Q: Как сделать резервную копию истории?
**A:** Скопируйте файл:
```
C:\Users\<Username>\CompasAnalyzer\history\analysis_history.json
→ На флешку или в облако
```

---

## 📝 История версий

### v2.0 (31.10.2025)
- ✅ Добавлен выбор типа устройства перед анализом
- ✅ Колонка "Тип устройства" в таблицах
- ✅ Фильтр и сортировка по типу устройства
- ✅ Переименование "Компас" → "№ ДСС"
- ✅ Исправлен просмотр неуспешных анализов
- ✅ Улучшена обработка ошибок (нет `undefined`)
- ✅ Оптимизация памяти для больших историй

### v1.x
- История анализов с фильтрацией
- Пакетная обработка с горутинами
- Редактор файлов
- Экспорт CSV/JSON
- Визуализация графиков

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте FAQ выше
2. Убедитесь, что используете последнюю версию
3. Проверьте логи в папке `logs/`

---

## 📄 Лицензия

© 2025 Compass Analyzer. Все права защищены.

---

**ГОТОВО К ИСПОЛЬЗОВАНИЮ!** 🎉

